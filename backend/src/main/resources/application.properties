spring.application.name=settleflow

spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/settleflow}
spring.datasource.username=${DATABASE_USERNAME:settleflow_app}
spring.datasource.password=${DATABASE_PASSWORD}

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false